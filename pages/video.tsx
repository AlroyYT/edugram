import React from 'react';
import Search from '../components/SearchByTopic';  // Adjust the import path based on your project structure

const Video: React.FC = () => {
  return <Search />;
};

export default Video;
